local class = require "class"
local define = require "define"
local script_base = require "script_base"
local owuliang_sdb = class("owuliang_sdb", script_base)
function owuliang_sdb:OnDefaultEvent(selfId,targetId)

end

function owuliang_sdb:OnDie(selfId,killerId)

end

return owuliang_sdb